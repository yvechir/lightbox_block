name: LightBox Block
description: Creates a block that will appear on every page of the website until a user clicks out of it.
package: Custom

type: module
core_version_requirement: ^8 || ^9
configure: lightbox_block.admin_settings
dependencies:
  - newsform
